apiVersion: 1

datasources:
  - name: InfluxDB_v3_InfluxQL
    type: influxdb
    access: proxy
    url: $INFLUXDB_URL
    jsonData:
      version: SQL
      dbName: $INFLUXDB_DATABASE_NAME
      httpMode: POST
      insecureGrpc: false
    secureJsonData:
      token: $INFLUXDB_AUTH_TOKEN