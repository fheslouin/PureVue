apiVersion: 1

datasources:
  - name: InfluxDB_v3_InfluxQL
    type: influxdb
    access: proxy
    url: http://influxdb:8181
    jsonData:
      version: SQL
      dbName: purevue
      httpMode: POST
      insecureGrpc: false
    secureJsonData:
      token: 'my-token'